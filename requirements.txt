# Dependencies for the component-model track scripts (measure_views, plan
# verification). The Blender scripts need no pip deps — they run inside
# Blender's bundled Python. The AI-mesh track has its own heavier set in
# requirements-shape.txt.
# Install with: python -m venv venv && venv/bin/pip install -r requirements.txt

Pillow>=10.0
opencv-python>=4.8
numpy>=1.24
trimesh>=4.0
scipy>=1.10
